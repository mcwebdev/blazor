version: '3.8'

services:
  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=flowboard
      - POSTGRES_PASSWORD=flowboard
      - POSTGRES_DB=flowboard
    ports:
      - "5432:5432"
